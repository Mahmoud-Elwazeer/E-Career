Django==4.2.16
djangorestframework==3.15.2
structlog==24.4.0
dj-database-url==2.2.0
djangorestframework-simplejwt==5.3.1
django-allauth==0.63.6
django-cors-headers==4.3.1
django-filter==24.2
drf-spectacular==0.27.2
whitenoise==6.7.0
python-decouple==3.8
Pillow==10.4.0
factory-boy==3.3.0

# Database
psycopg2-binary==2.9.9

# Job scraping
jobspy==0.31.0
requests==2.32.3  # >=2.32.3 required by docling 2.5.0; also satisfies cohere/typesense
httpx==0.27.0
beautifulsoup4==4.12.3
fake-useragent==1.4.0

# Playwright for JS-heavy ATS pages
playwright==1.45.0

# Task queue
celery==5.3.4
redis==5.0.1
django-celery-beat==2.5.0

# Cron expression parsing
croniter==2.0.1

# WebSocket support
channels==4.0.0
channels-redis==4.2.0
daphne==4.0.0

# Encryption for Rashid messages
cryptography==42.0.0
django-encrypted-model-fields==0.6.5

# AWS Bedrock
boto3==1.34.0

# Search
typesense==0.21.0

# Vector search (pgvector via Django ORM — no external vector DB needed)
# pgvector extension is managed at the PostgreSQL level

# Cohere embeddings for semantic search — see requirements/ml.txt
# (cohere conflicts with docling's transformers/tokenizers on a clean resolve;
#  installed separately, treated as optional at runtime)

# Graph queries use recursive CTEs (no external extension needed)

# Admin Dashboard
django-unfold==0.40.0
django-import-export==4.4.0
tablib[html,ods,xls,xlsx,yaml]
openpyxl==3.1.5

# CV Parsing
# docling (IBM Docling) is in requirements/ml.txt — heavy AI stack, optional,
# installed separately. pdfplumber is the always-available fallback parser.
pdfplumber==0.11.0  # PDF text extraction (primary fallback, no heavy deps)

# PDF Generation (resume export)
xhtml2pdf==0.2.16

# Monitoring & Observability
sentry-sdk==2.13.0  # Error tracking and monitoring
# easyocr (OCR for scanned CVs) is in requirements/ml.txt (pulls torch/CUDA)
python-docx==1.1.2  # DOCX parsing

# Recommendation Engine
# lightfm is OPTIONAL: the recommendation engine (apps/search/recommendation_engine.py)
# falls back gracefully when it is not installed. It is left commented because
# version 1.17.2 does not exist on PyPI (latest is 1.17) and lightfm requires a
# C build toolchain + Python < 3.11, which is not available on all hosts.
# To enable ML recommendations on a compatible host: pip install "lightfm==1.17"
# lightfm==1.17
# numpy/scipy/scikit-learn are imported at module load by search/skills/core
# services, so they stay in base (they do not conflict with each other).
# Pinned to the working production venv.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2

# Voice Interview / WebSocket
websockets==12.0
